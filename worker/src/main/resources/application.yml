server:
  port: 8090
logging:
  level:
    com.bnk: INFO
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost/ses
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update